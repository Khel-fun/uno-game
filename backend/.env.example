# Server configuration
PORT=4000
NODE_ENV=development

# Logging configuration
LOG_LEVEL=info  # Options: error, warn, info, http, verbose, debug, silly

# Diamnet configuration
DIAMNET_SECRET_KEY=your_secret_key_here

# Convex configuration (optional - for data storage only)
# Get your deployment URL from: https://dashboard.convex.dev
# You can also put this in .env.local (which is gitignored)
CONVEX_URL=https://your-deployment.convex.cloud
